import SwiftUI

extension Color {
    static let buttonsTitle = Color("buttonsTitle")
    static let card = Color("card")
    static let shadow = Color("shadow")
    static let text = Color("text")
}
